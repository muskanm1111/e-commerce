export const metadata = {
  title: "My Account | Power Supplements",
  description: "Manage your account, orders, and preferences",
};

export default function AccountLayout({ children }) {
  return children;
}
