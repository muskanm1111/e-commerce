export const metadata = {
  title: "Your Cart | Power Supplements",
  description: "Review and manage the items in your shopping cart",
};

export default function CartLayout({ children }) {
  return children;
}
